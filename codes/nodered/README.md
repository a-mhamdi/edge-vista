# Node-RED

Docker Compose setup for Node-RED, used to build IoT/edge data flows (with InfluxDB + dashboard support).

```bash
docker compose up -d
```

*UI:* `http://localhost:1880`

> [!NOTE]
> Flows and settings persist in `./data`.
