#pragma once

#include <cstdint>

namespace config
{
    constexpr char WIFI_SSID[]     = "<SSID>";
    constexpr char WIFI_PASSWORD[] = "<PASSWORD>";

    const int SERVER_PORT = 80;
    constexpr char ENDPOINT[] = "/capture";
}
